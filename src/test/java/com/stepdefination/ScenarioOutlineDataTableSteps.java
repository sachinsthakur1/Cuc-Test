package com.stepdefination;

import org.junit.Assert;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;

import cucumber.api.java.en.Given;
import cucumber.api.java.en.Then;
import cucumber.api.java.en.When;

public class ScenarioOutlineDataTableSteps 
{
	/*WebDriver driver;
	
	@Given("^I go to zoo adoption website$")
	public void i_go_to_zoo_adoption_website() throws Throwable {
		driver = new FirefoxDriver();
		driver.navigate().to("http://www.thetestroom.com/webapp");
	}
	
	@When("^I click on ([^\"]*)$")
	public void i_click_on(String link) throws Throwable {
		driver.findElement(By.id(link)).click();
	}

	@Then("^I should be on ([^\"]*) page$")
	public void i_should_be_on_page(String title) throws Throwable {
	    Assert.assertTrue("You are not on " + title, driver.getTitle().contains(title));
	}
	
	@Then("^I close the browser$")
	public void i_close_the_browser() throws Throwable {
	    driver.quit();
	}*/
}
